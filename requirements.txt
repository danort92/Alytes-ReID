# Core
torch>=2.0.0
torchvision>=0.15.0

# Detection — YOLO11 requires ultralytics>=8.3.0
ultralytics>=8.3.0

# Segmentation (SAM2 — installed from GitHub, not PyPI)
# pip install git+https://github.com/facebookresearch/sam2.git
# Uncomment the line above or install manually; excluded from pip install -r
# because it requires a full git clone + build.

# Re-ID / Metric Learning
pytorch-metric-learning>=2.3.0
timm>=0.9.0
faiss-cpu>=1.7.4

# Image processing
opencv-python>=4.8.0
albumentations>=1.3.0
Pillow>=10.0.0

# Web interface
gradio>=4.0.0

# Experiment tracking
mlflow>=2.8.0

# Data & config
pyyaml>=6.0
requests>=2.31.0
tqdm>=4.66.0
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Utilities
pathlib2>=2.3.0; python_version < "3.4"
scikit-learn>=1.3.0
scipy>=1.11.0
